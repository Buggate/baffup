<br />
<br />
<br />
<p style="font-family: 'Julius Sans One', sans-serif; font-size: 18px; color: black; font-weight: bold;">Change the occasion set date and time. </p><br />



 <%= form_for(@party, :url => {:action => 'update', :id => @party.id, :user_id => current_user.id}) do |f| %>


    <%= render(:partial => "date_form", :locals => {:f => f}) %>

 <% end %>

 <strong><%= link_to 'Back', party_path(:party_id => @party.id), :type => "button", :class => "btn btn-warning", :style => "font-family: 'Mogra', cursive; font-size: 14px; color: black;" %></strong><br>
