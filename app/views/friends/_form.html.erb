 <%= simple_form_for([@pouch, @friend, @pouch.friends.build({:email => profile.email, :pouch_id => @pouch.id, :user_id => @pouch.user.id, :buddy_id => profile.user.id })]) do |f| %>

         
     
           
            <%= f.text_field(:pouch_id) %>
      
            <%= f.text_field(:buddy_id) %>
      
            <%= f.text_field(:user_id) %>
       
            <%= f.text_field(:email) %>
       
            <%= f.text_field(:invite_id, :value => 0 ) %>

   <%= button_tag(type: 'submit' , class: "btn btn-success", :style => "font-family: 'Montserrat', sans-serif; font-size: 12px; font-weight: bold; color: #00193a; ") do %>
    <i class="fa fa-user-plus" aria-hidden="true" style="color: #00193a;"></i>&nbsp;send_request
  <% end %><br /><br />


<% end %>
