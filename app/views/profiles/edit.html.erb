
<div >

  <%= form_for(@profile, :url => {:action => 'update', :id => @profile.id, :user_id => current_user.id}) do |f| %>


   <%= render(:partial => "form", :locals => {:f => f}) %>

				 
	 <% end %>

	 <br /><br />

	 <strong><%= link_to 'Back', profiles_path, :type => "button", :class => "btn btn-warning", :style => "font-family: 'Julius Sans One', sans-serif; font-size: 16px; font-weight: bold; color: black;" %></strong>

</div>	 


	